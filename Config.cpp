#include <Windows.h>
#include <fstream>
#include <vector>
#include <iostream>

/*
	This is just default settings incase the settings file doesnt exist.

	Hostname is just the server address. Can change so it is nice to have a dynamic value
*/
const char config[] = "hostname = 192.168.0.2\n" "hostport = 4560\n" "debug = true\n"


/*
int createSettings(std::vector<std::string> strings, std::vector<int> ints)
{
	std::ifstream settingsFile;
	
}
*/

int changeSettings(std::vector<std::string> settings)
{
	std::ifstream settingsFile


	return 0;
}